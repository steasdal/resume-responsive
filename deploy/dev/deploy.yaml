apiVersion: apps/v1
kind: Deployment
metadata:
  name: resume-dev
  labels:
    app: resume-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resume-dev
  template:
    metadata:
      labels:
        app: resume-dev
    spec:
      containers:
      - name: resume
        image: steasdal/resume-responsive:latest-dev
        imagePullPolicy: Always

        ports:
        - name: resume-dev-port
          containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: resume-dev
  labels:
    app: resume-dev
spec:
  ports:
  - port: 80
    targetPort: resume-dev-port
    protocol: TCP
    name: resume-dev-port
  selector:
    app: resume-dev

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: resume-dev-ingress
spec:
  rules:
  - host: resume-dev.teasdale.link
    http:
      paths:
      - path: /
        backend:
          serviceName: resume-dev
          servicePort: resume-dev-port
